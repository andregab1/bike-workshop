import { getRequestContext } from "@/lib/request-context"; import { workOrderService } from "@/modules/work-orders/services/work-order-service"; import { errorResponse } from "@/shared/http/errors";
export async function POST(_request: Request, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; return Response.json({ data: await workOrderService.transition(await getRequestContext(), id, "reopen") }); } catch (error) { return errorResponse(error); } }
